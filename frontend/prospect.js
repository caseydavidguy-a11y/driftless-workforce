const params=new URLSearchParams(location.search);const wanted=(params.get("employer")||"").toLowerCase();
const demo=[{employer:"Great Lakes Cheese",score:73,priority:"Pursue",openings:3,industries:["manufacturing","skilled trades"],locations:["West Salem, WI"],status:"NEW",next_action:"Research HR/Talent Acquisition or plant leadership",reasons:["Multiple openings across target industries","Leadership hiring creates a stronger recruiting signal","Local hiring activity is concentrated in the Driftless service area"],momentum:{direction:"up",pct:167,change:5}},{employer:"Trane Technologies",score:68,priority:"Monitor",openings:2,industries:["manufacturing","operations"],locations:["La Crosse, WI"],status:"NEW",next_action:"Research HR/Talent Acquisition or operations leadership",reasons:["Multiple local operations/manufacturing openings","Leadership and planning roles can indicate broader staffing demand"],momentum:{direction:"up",pct:100,change:1}},{employer:"Wis-Pak",score:48,priority:"Monitor",openings:1,industries:["manufacturing"],locations:["La Crosse, WI"],status:"NEW",next_action:"Monitor hiring activity",reasons:["Active local manufacturing hiring"],momentum:{direction:"flat",pct:0,change:0}}];
function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}
function momentum(m){if(!m)return '';const label=m.direction==='up'?'Rising':m.direction==='down'?'Cooling':m.direction==='baseline'?'Baseline':'Stable';const sign=m.pct>0?'+':'';return `<span class="momentum ${m.direction}">${label}${m.direction!=='flat'&&m.direction!=='baseline'?` ${sign}${m.pct}%`:''}</span>`}
async function load(){let rows=demo;try{const r=await fetch("../data/employer_opportunities.json",{cache:"no-store"});if(r.ok){const d=await r.json();if(Array.isArray(d)&&d.length)rows=d;else if(Array.isArray(d.employers)&&d.employers.length)rows=d.employers}}catch(_){}const p=rows.find(x=>x.employer.toLowerCase()===wanted)||rows[0];document.title=`${p.employer} | Driftless Workforce`;document.getElementById("name").textContent=p.employer;document.getElementById("sub").innerHTML=`${esc((p.locations||[]).join(" · "))} · ${esc(p.status||"NEW")} ${momentum(p.momentum)}`;document.getElementById("detail").innerHTML=`<div class="card detail-card"><div><div class="priority ${p.priority.toLowerCase()}">${esc(p.priority)}</div><div class="bigscore">${p.score}<small>/100</small></div><h2>Why this prospect?</h2><ul>${(p.reasons||["Hiring activity matches Driftless Workforce target criteria."]).map(x=>`<li>${esc(x)}</li>`).join("")}</ul><h2>Hiring picture</h2><div class="signals"><span class="pill">${p.openings||0} openings</span>${(p.industries||[]).map(x=>`<span class="pill">${esc(x)}</span>`).join("")}${momentum(p.momentum)}</div><h2>Recommended approach</h2><p>${esc(p.next_action||"Research the appropriate decision-maker.")}</p></div><aside><h3>Prospect status</h3><strong>${esc(p.status||"NEW")}</strong><p class="meta">Keep the record evidence-backed. Do not mark a contact verified until a public source is captured.</p><button onclick="alert('Next action: '+${JSON.stringify(p.next_action||'Research decision-maker')})">Next action</button></aside></div>`}load();
