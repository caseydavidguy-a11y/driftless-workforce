const params=new URLSearchParams(location.search);const wanted=(params.get("employer")||"").toLowerCase();
const demo=[
 {employer:"Great Lakes Cheese",score:73,priority:"Pursue",openings:3,industries:["manufacturing","skilled trades"],locations:["West Salem, WI"],status:"NEW",next_action:"Research HR/Talent Acquisition or plant leadership",reasons:["Multiple openings across target industries","Leadership hiring creates a stronger recruiting signal","Local hiring activity is concentrated in the Driftless service area"]},
 {employer:"Trane Technologies",score:68,priority:"Monitor",openings:2,industries:["manufacturing","operations"],locations:["La Crosse, WI"],status:"NEW",next_action:"Research HR/Talent Acquisition or operations leadership",reasons:["Multiple local operations/manufacturing openings","Leadership and planning roles can indicate broader staffing demand"]},
 {employer:"Wis-Pak",score:48,priority:"Monitor",openings:1,industries:["manufacturing"],locations:["La Crosse, WI"],status:"NEW",next_action:"Monitor hiring activity",reasons:["Active local manufacturing hiring"]}
];
function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}
async function load(){let rows=demo;try{const r=await fetch("../data/employer_opportunities.json",{cache:"no-store"});if(r.ok){const d=await r.json();if(Array.isArray(d)&&d.length)rows=d;else if(Array.isArray(d.employers)&&d.employers.length)rows=d.employers}}catch(_){}const p=rows.find(x=>x.employer.toLowerCase()===wanted)||rows[0];document.title=`${p.employer} | Driftless Workforce`;document.getElementById("name").textContent=p.employer;document.getElementById("sub").textContent=`${(p.locations||[]).join(" · ")} · ${p.status||"NEW"}`;document.getElementById("detail").innerHTML=`<div class="card detail-card"><div><div class="priority ${p.priority.toLowerCase()}">${esc(p.priority)}</div><div class="bigscore">${p.score}<small>/100</small></div><h2>Why this prospect?</h2><ul>${(p.reasons||["Hiring activity matches Driftless Workforce target criteria."]).map(x=>`<li>${esc(x)}</li>`).join("")}</ul><h2>Hiring picture</h2><div class="signals"><span class="pill">${p.openings||0} openings</span>${(p.industries||[]).map(x=>`<span class="pill">${esc(x)}</span>`).join("")}</div><h2>Recommended approach</h2><p>${esc(p.next_action||"Research the appropriate decision-maker.")}</p></div><aside><h3>Prospect status</h3><strong>${esc(p.status||"NEW")}</strong><p class="meta">Keep the record evidence-backed. Do not mark a contact verified until a public source is captured.</p><button onclick="alert('Next action: '+${JSON.stringify(p.next_action||'Research decision-maker')})">Next action</button></aside></div>`}load();
