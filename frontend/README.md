# Driftless Workforce Command Center

Open `index.html` to use the V1 browser interface.

The interface attempts to load `../data/employer_opportunities.json`. If live output is unavailable, it falls back to clearly identifiable demo records so the UI remains usable during development.

Production deployment should publish this directory with GitHub Pages or another static host.
