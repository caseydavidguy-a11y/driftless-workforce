function renderTrend(snapshots,name){const wanted=name.toLowerCase();const points=snapshots.map(s=>{const e=(s.employers||[]).find(x=>(x.slug||'').toLowerCase()===wanted||(x.name||'').toLowerCase()===wanted);return e?{date:s.captured_at,count:e.opening_count??e.openings??0}:null}).filter(Boolean);if(!points.length)return '<div class="empty">No historical hiring snapshots are available for this employer yet.</div>';const max=Math.max(...points.map(p=>p.count),1);return `<div class="trend-summary"><strong>${points[points.length-1].count}</strong><span>current openings</span><span class="trend-change">${points.length>1?((points[points.length-1].count-points[0].count)>=0?'+':'')+(points[points.length-1].count-points[0].count)+' since first snapshot':'Baseline captured'}</span></div><div class="trend-chart" role="img" aria-label="Hiring openings over time">${points.map(p=>`<div class="trend-point"><div class="bar" style="height:${Math.max(8,Math.round((p.count/max)*100))}%"><span>${p.count}</span></div><small>${new Date(p.date).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</small></div>`).join('')}</div>`}
async function loadTrend(){const name=new URLSearchParams(location.search).get('employer')||'';if(!name){document.getElementById('trend').innerHTML='<div class="empty">Select an employer to view hiring history.</div>';return}try{const r=await fetch('./data/snapshot_history.json',{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const d=await r.json();const snapshots=Array.isArray(d)?d:(Array.isArray(d.snapshots)?d.snapshots:[]);document.getElementById('trend').innerHTML=renderTrend(snapshots,name)}catch(error){document.getElementById('trend').innerHTML='<div class="empty">Hiring history is unavailable until a successful snapshot refresh is published.</div>'}}loadTrend();
