async function loadSignals(){let signals=[];try{const r=await fetch('./data/signals.json',{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const d=await r.json();signals=Array.isArray(d)?d:(Array.isArray(d.signals)?d.signals:[])}catch(_){}const root=document.getElementById('alerts');if(!root)return;root.innerHTML=signals.length?signals.map(s=>`<article class="alert ${s.severity}"><div><div class="alert-kicker">${String(s.severity).toUpperCase()} SIGNAL · ${String(s.kind).replaceAll('_',' ')}</div><strong>${escapeHtml(s.employer)}</strong><p>${escapeHtml(s.message)}</p></div><span>${escapeHtml(s.metric||'')}</span></article>`).join(''):'<div class="empty">No new hiring signals.</div>'}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}loadSignals();
